<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешаем системе находить 2ГИС и обработчики geo: (Android 11+) -->
    <queries>
        <package android:name="ru.dublgis.dgismobile" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Widget">

        <!-- Главное окно приложения (лаунчер) -->
        <activity
            android:name="ru.gishackathon.widget.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Конфигурационный экран виджета (открывается хост-лаунчером) -->
        <activity
            android:name="ru.gishackathon.widget.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Провайдер виджета -->
        <receiver
            android:name="ru.gishackathon.widget.TransportWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/transport_widget_info" />
        </receiver>

    </application>
</manifest>
